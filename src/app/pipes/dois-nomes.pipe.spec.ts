import { DoisNomesPipe } from './dois-nomes.pipe';

describe('DoisNomesPipe', () => {
  it('create an instance', () => {
    const pipe = new DoisNomesPipe();
    expect(pipe).toBeTruthy();
  });
});
